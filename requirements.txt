# Ядро API
fastapi>=0.115.0
uvicorn[standard]>=0.27.0

# Валидация (v2 требует pydantic-settings)
pydantic>=2.7.0
pydantic-settings>=2.2.0

# Векторный движок (txtai 7.x актуален для Python 3.11)
txtai[pipeline]>=7.0.0
# torch 2.x обязателен для Python 3.11 на Windows
torch>=2.0.0

# Инструменты
python-dotenv>=1.0.1
loguru>=0.7.2
